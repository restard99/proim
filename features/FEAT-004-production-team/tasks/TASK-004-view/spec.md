# TASK-004: 생산일지 화면

## 목적
02-design.html 목업대로 왼쪽 업로드+목록, 오른쪽 공정별 탭+표를 보여주는 화면을 구현한다. 표는 컬럼을 하드코딩하지 않고 선택된 시트의 headers/rows를 그대로 렌더링한다.

## 작업 범위
- 생성할 파일: `components/production/ProductionLogView.tsx`, `app/(app)/production-logs/page.tsx`

## 완료 기준
- [ ] 왼쪽: 업로드 입력(생산팀 전체가 접근하므로 항상 노출) + 기간(period_label) 내림차순 목록, 항목 클릭 시 상세 로드
- [ ] 오른쪽: 선택한 로그의 시트 이름들이 탭으로 표시, 탭 클릭 시 해당 시트의 헤더/행을 표로 렌더링(범용 컴포넌트, 컬럼 고정 없음)
- [ ] 원본 파일 열기 버튼(서명 URL)
- [ ] 삭제 버튼(본인이 올린 것 또는 admin일 때만 노출)
- [ ] 빈 상태("등록된 생산일지가 없습니다" / "왼쪽에서 생산일지 엑셀을 업로드해주세요")
- [ ] 업로드 실패 시 오류 메시지 표시
- [ ] `/production-logs` 페이지: 생산팀 또는 admin만 접근, 그 외는 홈으로 리다이렉트
