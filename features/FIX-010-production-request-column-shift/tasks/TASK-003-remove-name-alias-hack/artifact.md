# TASK-003: 부자재 매칭 - 낡은 "천일염" 별칭 치환 규칙 제거 — 아티팩트

## 상태: 완료

## 수정 내용
`NAME_ALIAS_RULES`(제품명에 "천일염"이 있으면 무조건 " 한여름눈꽃천일염굵은소금 "으로 치환하던 규칙)를 제거했다. 이 규칙은 예전에 생산의뢰서 제품명이 축약형("샘표 꽃소금 500g")이던 시절 특정 샘표 제품 하나를 매칭시키려던 임시방편이었는데, 모든 "천일염" 포함 제품명에 무조건 적용되게 짜여 있어서, 제품명을 Y-ERP 정식명과 정확히 맞춘 지금은 오히려 이미 정확히 일치하던 이름을 깨뜨리고 있었다.

## 수정된 파일
- `lib/yerp/production-materials.ts`: `NAME_ALIAS_RULES`/`applyNameAliases` 삭제, 토큰화 시 원본 제품명 그대로 사용

## 완료 기준 확인
- [x] 실제 `★생산의뢰서(0902).xlsx`(69개 제품)로 Y-ERP 완제품 목록과 직접 대조 검증
  - 별칭 규칙 있음(수정 전): 11/69 매칭
  - 별칭 규칙 제거(수정 후): 31/69 매칭 — 정확히 그 20개가 별칭 규칙 때문에 깨졌던 것과 일치, 회귀 없이 순증가
- [x] `npx tsc --noEmit`, `npm run build` 통과

## 이슈 및 결정사항
남은 38개(0902 파일 기준) 미매칭/애매함 항목은 이번 수정 범위 밖이다. 그중 일부는 후보가 2~3개로 애매(같은 이름의 내수/수출/연도별 변형이 여러 개 등록됨)하고, 일부는 후보 0개(Y-ERP에 아직 그 정확한 이름의 완제품이 없거나 "해표천일염가는입자 10kg"처럼 엑셀 쪽 공백 표기 차이)다. 억지로 새 별칭 규칙을 추가하기보다, 실제로 더 필요한 사례가 쌓이면 그때 근거를 가지고 다뤄야 한다고 판단해 이번엔 손대지 않았다.
