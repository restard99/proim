# TASK-002: 워크북 파서 — 아티팩트

## 상태: 배포 완료

## 구현 내용
`parseReportWorkbookTargets()`를 작성했다. "팀보고" 시트의 마감일자 라벨을 찾아 날짜를 얻고, 매출 3개 시트의 일자별 블록에서 그 날짜 행을 찾아 태평소금/태평염전/박물관 법인 목표(주간+월간)와 섬들채 업장별(6개+박물관) 목표·실적을 뽑는다. 생산목표(천일염/가공염, 월간만)는 생산-소금/생산-염전의 월별 매트릭스에서 라벨 기준으로 추출한다.

## 생성된 파일
- `lib/executive/parse-report-workbook-targets.ts`

## 완료 기준 확인
- [x] 마감일자 라벨 기준 파싱 (2026-09-06 정확히 추출)
- [x] 일자별 블록 매칭 (4개 법인 + 6개 업장 + 박물관)
- [x] 섬들채 "합계"는 6개 업장 실측값의 직접 합산으로 계산
- [x] 생산목표(월간) 추출 — 태평소금 천일염/가공염 12개월, 태평염전 8개월(3~10월, 실제 값 있는 달만)
- [x] 실제 파일 검증: PPT 18번 슬라이드의 모든 값(소금가게~박물관, 계획/실적/주간/월간)과 정확히 일치 확인
- [x] `npx tsc --noEmit`, `npx eslint` 통과

## 이슈 및 결정사항
실제 파일을 컴파일 후 직접 실행해 검증하는 과정에서, 매출-서비스1 시트의 "전체" 블록(X열)이 계획값은 6개 업장 합과 정확히 일치하지만 실적값은 박물관 실적까지 포함된 값(차이가 정확히 박물관의 주간/월간 실적과 일치)이 들어있는 원본 데이터 불일치를 발견했다. 이를 그대로 쓰면 화면의 "합계" 행이 그 위에 나열된 6개 업장의 합과 안 맞아 보이므로, "전체"(합계) 행은 원본 블록을 읽지 않고 6개 업장 값을 직접 합산해서 계산하도록 했다.
