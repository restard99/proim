# TASK-012: 주간업무보고 날짜 지정 조회

## 목적
주간업무보고 조회 방식을 지난주/다음주 버튼 기본에서, 날짜를 직접 지정해 원하는 주로 바로 이동할 수 있도록 보완한다.

## 작업 범위
- 수정할 파일: `components/executive/WeeklyReportView.tsx`

## 완료 기준
- [ ] "← 지난 주" / "다음 주 →" 버튼은 기존 그대로 동작
- [ ] 날짜 선택(date picker) 추가 — 선택한 날짜가 속한 주(월요일 시작)로 바로 이동
- [ ] 날짜 선택 UI는 현재 조회 중인 주의 월요일을 표시
- [ ] `npx tsc --noEmit`, `npx eslint` 통과
