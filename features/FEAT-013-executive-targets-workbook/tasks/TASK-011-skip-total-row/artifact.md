# TASK-011: 원시 판매 데이터 파일 끝의 "합계" 행 무시 — 아티팩트

## 상태: 완료

## 구현 내용
사용자가 새로 받은 "일자별(상품별)" 파일에는 맨 끝(692행)에 "합계" 요약 행이 붙어 있었다. 파서가 이 행의 일자 칸을 날짜로 못 읽어 오류로 처리했는데, 일자 칸이 정확히 "합계"인 행은 데이터가 아니라 요약이므로 조용히 건너뛰도록 고쳤다.

## 수정된 파일
- `lib/executive/parse-seomdeulchae-sales-raw.ts`: 일자 칸이 "합계"인 행 스킵 처리 추가

## 완료 기준 확인
- [x] "합계" 행 무시
- [x] 실제 파일 검증: 692행 중 685개 데이터 행 정상 파싱, 마지막 행 확인
- [x] `npx tsc --noEmit`, `npx eslint`, `npm run build` 통과
