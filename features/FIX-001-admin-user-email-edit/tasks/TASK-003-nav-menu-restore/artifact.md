# TASK-003: 사이드바 관리자 메뉴에 "가입 승인 관리" 복구 — 아티팩트

## 상태: 배포 완료

## 수정 내용
`ADMIN_NAV_ITEMS`에 "가입 승인 관리"(`/admin/approvals`) 항목을 다시 추가했다. FEAT-007 브랜치에서 한 번 고쳤지만 그건 `dev`에 아직 병합 전이라, `dev`에서 새로 딴 이 FIX 브랜치엔 반영이 안 되어 있었다.

## 수정된 파일
- `components/layout/nav-items.ts`: `ADMIN_NAV_ITEMS`에 "가입 승인 관리" 추가(맨 위)

## 완료 기준 확인
- [x] 관리자 사이드바에 "가입 승인 관리", "시스템검토 게시판" 둘 다 표시, `npx tsc --noEmit` 통과

## 이슈 및 결정사항
같은 수정이 서로 다른 브랜치(FEAT-007, 이 FIX)에 중복으로 들어가게 됐다 — 나중에 두 브랜치를 `dev`에 병합할 때 `nav-items.ts`가 겹칠 수 있으니, 병합 순서와 충돌 여부를 8단계 배포 시 확인해야 한다.
